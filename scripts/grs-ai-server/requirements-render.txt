fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Object detection + tracking (no pose — saves ~1.5 GB RAM on Render)
ultralytics>=8.2.0
supervision>=0.20.0

# Vision + tensor libraries — CPU-only torch (700 MB vs 2.5 GB for GPU)
opencv-python-headless>=4.9.0
Pillow>=10.3.0
numpy>=1.26.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.3.0
torchvision>=0.18.0
