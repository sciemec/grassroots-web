// Firebase removed — auth is handled by Laravel JWT.
// This stub keeps existing imports from breaking at build time.
export const auth = { currentUser: null };
export const googleProvider = {};
const app = {};
export default app;
